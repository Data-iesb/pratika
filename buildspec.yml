version: 0.2
phases:
  build:
    commands:
      - aws s3 sync . s3://www.pratika.dataiesb.com --delete --exclude ".git/*" --exclude "buildspec.yml"
  post_build:
    commands:
      - aws cloudfront create-invalidation --distribution-id E3SBG0LH2IEEXM --paths "/*"
